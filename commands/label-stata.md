Find all .xlsx files in the current working directory that look like XLSForm / SurveyCTO form definitions (they should have sheets named "survey", "choices", and "settings"). Also find all .csv data files in the current directory. For each form definition found, use the surveycto-form-reader skill to parse it. Then, using the data-first workflow: read the column headers from the corresponding .csv data file first, left-join against the parsed survey and choices sheets, and generate a Stata .do file that defines and assigns value labels for all select_one and select_multiple variables that actually exist in the data. Respect Stata's 32-character variable name limit — use truncated names throughout and never attempt to rename beyond 32 characters. Use `capture` before `label values` commands for safety. Save the .do file as {original_filename}_labels.do in the current directory. At the end, list all .do files created.
