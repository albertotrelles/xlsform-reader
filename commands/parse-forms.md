Find all .xlsx files in the current working directory. For each one, check if it looks like an XLSForm / SurveyCTO form definition by verifying it has sheets named "survey", "choices", and "settings". For each valid form definition found, use the surveycto-form-reader skill to parse it and generate a detailed summary including: form title, form ID, sections, field counts by type, choice lists, repeat groups, and a full variable listing. Save each summary as a markdown file named {original_filename}_summary.md in the current directory. Skip any .xlsx files that are not form definitions. At the end, list all summaries created and note any .xlsx files that were skipped.
